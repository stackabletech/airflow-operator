---
volumePermissions:
  enabled: false
  securityContext:
    runAsUser: auto

securityContext:
  enabled: false

shmVolume:
  chmod:
    enabled: false

containerSecurityContext:
  enabled: false

auth:
  username: airflow
  password: airflow
  database: airflow
