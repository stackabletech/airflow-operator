---
volumePermissions:
  enabled: false
  securityContext:
    runAsUser: auto

primary:
  podSecurityContext:
    enabled: true
  containerSecurityContext:
    enabled: false

shmVolume:
  chmod:
    enabled: false

auth:
  username: airflow
  password: airflow
  database: airflow
